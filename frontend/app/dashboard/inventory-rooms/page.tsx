"use client";
import { DashboardHeader } from "@/components";

const page = () => {
    return (
        <div>
            <DashboardHeader title="Rooms" />
        </div>
    );
};

export default page;
